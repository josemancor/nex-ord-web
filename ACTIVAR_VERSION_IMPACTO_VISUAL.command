#!/bin/bash
# Doble clic en Mac Finder para activar inmediatamente la Versión de Impacto Visual
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
"$DIR/ACTIVAR_VERSION_IMPACTO_VISUAL.sh"
read -p "Presiona ENTER para cerrar..."
